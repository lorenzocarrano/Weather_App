Attenzione: per motivi di privacy non ho incluso il mio token di accesso per l'API del sito di previsioni del meteo su cui l'App si appoggia; pertanto, per lanciare l'app sarà necessario registrarsi al sito ed ottenere il proprio token di accesso univoco, o sostituire il servizio API con uno a piacere (in questo caso modificando adeguatamente il contenuto del file json_analysis.py, che contiene la dichiarazione delle funzioni con le quali l'app interpreta il ritorno della chiamata all'API).